# Shadow AI Sanitizer (Core Engine)

Lightweight, zero-dependency Python sanitizer for intercepting and masking credentials/PII before transmission to LLM providers.

## Getting Started

1. **Install Dependencies**
   ```bash
   pip install -r requirements.txt